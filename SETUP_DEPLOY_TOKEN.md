# 🔐 Настройка DEPLOY_TOKEN для автоматического deployment

## ⚠️ КРИТИЧНО: Без этого токена deployment НЕ БУДЕТ РАБОТАТЬ!

Новый workflow `.github/workflows/deploy-to-root.yml` требует токен для push в `alubiama.github.io`.

---

## 📝 Пошаговая инструкция (5 минут):

### Шаг 1: Создать Personal Access Token на GitHub

1. **Открыть:** https://github.com/settings/tokens

2. **Кликнуть:** "Generate new token" → **"Generate new token (classic)"**

3. **Заполнить:**
   - **Note:** `StillBASING Deploy Token`
   - **Expiration:** `90 days` (или `No expiration` если хотите)

4. **Выбрать permissions:**
   - ✅ **repo** (полный доступ к репозиториям)
     - ✅ repo:status
     - ✅ repo_deployment
     - ✅ public_repo
     - ✅ repo:invite
     - ✅ security_events

5. **Кликнуть:** "Generate token" (зеленая кнопка внизу)

6. **ВАЖНО:** Скопировать токен! Он показывается **ТОЛЬКО ОДИН РАЗ**
   ```
   ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```
   Сохраните его временно в блокнот.

---

### Шаг 2: Добавить токен в Secrets репозитория

1. **Открыть:** https://github.com/Alubiama/StillBASING/settings/secrets/actions

2. **Кликнуть:** "New repository secret"

3. **Заполнить:**
   - **Name:** `DEPLOY_TOKEN` (ТОЧНО так, как написано!)
   - **Secret:** вставить скопированный токен

4. **Кликнуть:** "Add secret"

5. **Проверить:** В списке Secrets должен появиться `DEPLOY_TOKEN`

---

### Шаг 3: Merge и Push

```bash
# Перейти на ветку
git checkout claude/review-blockers-nS46J

# Запушить изменения
git push origin claude/review-blockers-nS46J

# Создать Pull Request на GitHub
# https://github.com/Alubiama/StillBASING/pull/new/claude/review-blockers-nS46J

# Смержить PR в main
```

---

### Шаг 4: Проверить что workflow запустился

1. **Открыть:** https://github.com/Alubiama/StillBASING/actions

2. **Найти:** "Deploy to Root Domain" workflow

3. **Проверить:** Статус должен быть ✅ зеленый

4. **Если красный ❌:**
   - Кликнуть на workflow
   - Посмотреть логи
   - Проверить что DEPLOY_TOKEN правильный

---

### Шаг 5: Проверить результат

**Подождать 2-3 минуты**, потом проверить:

1. **Репозиторий alubiama.github.io:**
   ```
   https://github.com/Alubiama/alubiama.github.io
   ```
   Должен появиться новый коммит: "Deploy from StillBASING ..."

2. **Открыть приложение:**
   ```
   https://alubiama.github.io/
   ```
   Должна загрузиться игра Still Basing

3. **Проверить Base SDK в консоли:**
   - Открыть DevTools (F12)
   - Перейти в Console
   - Обновить страницу
   - Должно показать: "Base SDK initialized: ..."

4. **Проверить в Base Dashboard:**
   - Открыть ваше приложение в Base
   - Ошибка "Ready not called" должна **исчезнуть**!

---

## 🎯 Что делает новый workflow?

```yaml
1. Checkout кода из StillBASING
2. npm install - установка зависимостей
3. npm run build - сборка с Base SDK
4. cd dist - перейти в собранные файлы
5. git init - инициализировать git
6. git push - запушить в alubiama.github.io
```

**Результат:**
- Base SDK автоматически попадает в корень
- Farcaster может читать манифест из `/.well-known/`
- Приложение работает по `https://alubiama.github.io/`

---

## ⚠️ Важные моменты:

### 1. Токен должен иметь доступ к alubiama.github.io
Если репозиторий приватный - токен должен иметь `repo` permission.

### 2. Secret Name должно быть ТОЧНО `DEPLOY_TOKEN`
Workflow ищет `${{ secrets.DEPLOY_TOKEN }}` - если название другое, не сработает!

### 3. Workflow запускается только на push в main
После merge PR в main - workflow автоматически запустится.

### 4. Старый workflow останется
Файл `.github/workflows/deploy.yml` оставлен для резерва.
Можно удалить если новый работает.

---

## 🐛 Troubleshooting:

### Ошибка: "Permission denied"
**Причина:** Токен не имеет прав на alubiama.github.io

**Решение:**
1. Создать новый токен с `repo` permission
2. Обновить DEPLOY_TOKEN в Secrets

---

### Ошибка: "Authentication failed"
**Причина:** Токен неправильный или истек

**Решение:**
1. Проверить что токен скопирован полностью
2. Создать новый токен если истек
3. Обновить DEPLOY_TOKEN

---

### Workflow не запускается
**Причина:** Не смержен в main

**Решение:**
```bash
git checkout main
git merge claude/review-blockers-nS46J
git push origin main
```

---

### alubiama.github.io не обновляется
**Причина:** Workflow упал с ошибкой

**Решение:**
1. Открыть https://github.com/Alubiama/StillBASING/actions
2. Найти красный ❌ workflow
3. Кликнуть и посмотреть логи
4. Исправить ошибку

---

## 📊 Проверочный чеклист:

Перед тем как тестировать в Base:

- [ ] DEPLOY_TOKEN создан на GitHub
- [ ] DEPLOY_TOKEN добавлен в Secrets
- [ ] Изменения запушены в ветку
- [ ] PR создан и смержен в main
- [ ] Workflow "Deploy to Root Domain" запустился
- [ ] Workflow завершился успешно (✅ зеленый)
- [ ] В alubiama.github.io появился новый коммит
- [ ] Сайт открывается: https://alubiama.github.io/
- [ ] В консоли видно "Base SDK initialized"

**Если все ✅ - открывайте в Base и проверяйте!**

---

## 🎉 После успешного deployment:

1. Обновить Base Dashboard (кнопка Refresh)
2. Подождать 5-10 минут (кэш)
3. Открыть приложение в Base
4. Ошибка "Ready not called" должна исчезнуть!
5. Валидировать на https://base.dev/preview
6. Подать на Featured! 🚀

---

**Удачи! Это последний шаг перед Featured!** 🎯
