# 🚀 Auto-Deploy Setup

## Что это?

GitHub Actions будет автоматически билдить и деплоить на `alubiama.github.io` при каждом пуше в `main` или `claude/push-docs-TPS1o`!

## 📝 Настройка (один раз)

### 1. Создай Personal Access Token

1. Иди на https://github.com/settings/tokens
2. Нажми **"Generate new token"** → **"Generate new token (classic)"**
3. Дай название: `StillBASING Deploy Token`
4. Выбери срок действия: **No expiration** (или какой хочешь)
5. Выбери разрешения:
   - ✅ `repo` (Full control of private repositories)
   - Это даст доступ к `alubiama.github.io`
6. Нажми **"Generate token"**
7. **СКОПИРУЙ TOKEN** (он покажется только один раз!)

### 2. Добавь Token в StillBASING Repository

1. Иди на https://github.com/Alubiama/StillBASING/settings/secrets/actions
2. Нажми **"New repository secret"**
3. Name: `DEPLOY_TOKEN`
4. Secret: **вставь скопированный token**
5. Нажми **"Add secret"**

### 3. Готово! 🎉

Теперь при каждом пуше в `main` или `claude/push-docs-TPS1o`:
- GitHub Actions автоматически запустится
- Установит зависимости
- Соберет проект (`npm run build`)
- Задеплоит в `alubiama.github.io`
- Ты получишь уведомление об успехе/ошибке

## 🔍 Как проверить что работает?

1. После настройки, запуш любое изменение:
   ```bash
   git add .
   git commit -m "Test auto-deploy"
   git push origin claude/push-docs-TPS1o
   ```

2. Иди на https://github.com/Alubiama/StillBASING/actions
3. Увидишь запущенный workflow "Deploy to alubiama.github.io"
4. Через 2-3 минуты проверь https://alubiama.github.io/

## 🔧 Ручной запуск

Можешь запустить деплой вручную:
1. Иди на https://github.com/Alubiama/StillBASING/actions/workflows/deploy.yml
2. Нажми **"Run workflow"**
3. Выбери branch и нажми **"Run workflow"**

## ❗ Важно

- Token должен иметь доступ к `alubiama.github.io` репозиторию
- Не делись токеном ни с кем!
- Если токен утечет - сразу удали его и создай новый
- Workflow будет запускаться только при пуше в указанные ветки

## 📊 Что происходит при деплое?

1. ✅ Checkout кода из StillBASING
2. ✅ Установка Node.js 20
3. ✅ Установка зависимостей (npm ci)
4. ✅ Сборка проекта (npm run build)
5. ✅ Проверка манифеста
6. ✅ Push в alubiama.github.io
7. ✅ GitHub Pages автоматически задеплоит новую версию

## 🎯 Результат

Больше не нужно:
- ❌ Вручную копировать файлы
- ❌ Запускать build локально
- ❌ Пушить в alubiama.github.io

Просто пушь в StillBASING и все деплоится автоматически! 🚀
